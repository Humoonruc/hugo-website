---
title: "3D Graph"
author: "Humoonruc"
date: "2020/11/16"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
library(plotly)

x <- seq(0, 100, 5)
y <- seq(0, 100, 5)
z <- seq(15, 19, 1)

z1 <- outer(x, y, FUN = function(x, y) 0.02 * x + 0.015 * y - 0.00008 * x * y + 0.00007 * x^2 - 0.00002 * y^2 + 15)

z2 <- outer(x, y, FUN = function(x, y) 0.02 * x + 0.015 * y - 0.00008 * x * y + 0.00007 * x^2 - 0.00002 * y^2 + 16)

plot_ly(showscale = FALSE) %>%
  add_surface(x = x, y = y, z = ~z1, color = "black") %>%
  add_surface(x = x, y = y, z = ~z2, colors = "red")
```
